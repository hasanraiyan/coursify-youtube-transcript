export async function logUsage(/* _opts */) {
  // No persistent storage. We currently just log the usage to console for observability.
  // Expand this to record to analytics/tracing if needed.
  return;
}
